# Postgres (utilisé par gen-secrets.sh et par le backend)
POSTGRES_USER=todoapp
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=todos

# Docker Hub : username où sont publiées les images todo-front / todo-back
# (utilisé par scripts/build-and-push.sh)
DOCKERHUB_USER=lukasambry
